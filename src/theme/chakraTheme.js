// chakra-theme.js (or any other file where you define Chakra UI theme)
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    accentPurple: "#ea80fc",
  },
});

export default theme;
